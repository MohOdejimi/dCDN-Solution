dCDN is a decentralized content-delivery network designed to distribute and retrieve content from nodes registered on the network using a peer-to-peer storage system and content-addressed identifiers.

Content is published to the network and splited into smaller chunks, replicated across multiple nodes, and retrieved in parallel using a content identifier.